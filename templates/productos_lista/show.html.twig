{% extends 'base.html.twig' %}

{% block title %}ProductosLista{% endblock %}

{% block body %}
    <h1>ProductosLista</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ productos_listum.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_productos_lista_index') }}">back to list</a>

    <a href="{{ path('app_productos_lista_edit', {'id': productos_listum.id}) }}">edit</a>

    {{ include('productos_lista/_delete_form.html.twig') }}
{% endblock %}
