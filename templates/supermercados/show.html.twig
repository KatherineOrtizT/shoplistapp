{% extends 'base.html.twig' %}

{% block title %}Supermercados{% endblock %}

{% block body %}
    <h1>Supermercados</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ supermercado.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ supermercado.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_supermercados_index') }}">back to list</a>

    <a href="{{ path('app_supermercados_edit', {'id': supermercado.id}) }}">edit</a>

    {{ include('supermercados/_delete_form.html.twig') }}
{% endblock %}
